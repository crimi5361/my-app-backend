node_modules/
.env
.env.*
!.env.example
RRR/
uploads/
*.log
config/secrets/
CLAUDE.md
.claude/

# Documentation interne — usage exclusif équipe de développement, jamais publiée
# sur le dépôt distant (voir README.md, section Déploiement).
docs/
documentation/
analyse/
notes/

# migrations/ n'est plus ignoré : sans les scripts de schéma, une copie du dépôt
# ne peut pas être mise en service ni testée. Aucun secret n'y figure — le mot de
# passe du rôle de lecture est généré à l'exécution par setup-role-assistant.js
# et affiché une seule fois, jamais écrit dans un fichier.
